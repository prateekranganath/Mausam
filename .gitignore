.env
__pycache__/
*.pyc
.venv/
venv/
.claude/

# Large raw dataset — kept locally per project instructions (never
# modified), not committed to keep the repo lightweight. Trained model
# artifacts are on Hugging Face (each model's README.md/model card);
# .joblib files are reproducible via scripts/train_model.py and
# scripts/train_all_india_model.py. metadata/evaluation JSON and model
# cards are small and worth diffing, so those stay tracked.
Data/india_weather_rainfall_data.xlsx
Data/processed/
models/**/*.joblib
models/**/plots/

*.log
.pytest_cache/
