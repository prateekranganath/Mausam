.env
__pycache__/
*.pyc
.venv/
venv/
.claude/

# Large raw dataset — kept locally per project instructions (never
# modified), not committed to keep the repo lightweight. Trained model
# artifacts are on Hugging Face (see models/README.md); .joblib files are
# reproducible via scripts/train_model.py.
Data/india_weather_rainfall_data.xlsx
Data/processed/
models/*.joblib
models/plots/

*.log
.pytest_cache/
